<?xml version="1.0" encoding="ASCII"?>
<routes xmlns="http://camel.apache.org/schema/spring">
    <route>
        <from uri="switchyard://ExternalInvocationService"/>
        <log message="ExternalInvokerService - message received: ${body}"/>
        <to uri="switchyard://ServiceInvoker"/>
    </route>
</routes>