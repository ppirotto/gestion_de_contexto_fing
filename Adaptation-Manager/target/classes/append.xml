<?xml version="1.0" encoding="ASCII"?>
<routes xmlns="http://camel.apache.org/schema/spring">
    <route>
        <from uri="switchyard://AppendService"/>
        <setBody>
            <simple>${body}</simple>
        </setBody>
        <log message="AppendService - message received: ${body}"/>
    </route>
</routes>