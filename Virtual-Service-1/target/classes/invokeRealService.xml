<?xml version="1.0" encoding="ASCII"?>
<routes xmlns="http://camel.apache.org/schema/spring">
    <route>
        <from uri="switchyard://ContextAwareService"/>
        <log message="ContextAwareService - message received: ${body}"/>
        <to uri="switchyard://RealService"/>
    </route>
</routes>